name: Shell Commands 

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:
  run-shell-command:
    runs-on: ubuntu-latest
    steps: 
      - name: echo a string
        run: echo "My name is Lalit Pratap"
        timeout-minutes: 0
      - name: multiline script 
        run: |
           node -v 
           npm -v